{
  "buildCommand": "cd ServeSync && npm run build",
  "outputDirectory": "ServeSync/dist",
  "installCommand": "cd ServeSync && npm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}